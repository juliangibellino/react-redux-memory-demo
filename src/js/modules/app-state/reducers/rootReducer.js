/**
 * Root reducer for an AppState
 * 
 * TODO: currently this is just a reducer stub. Determine what functionality is needed at the root of the app state
 */
export default function rootReducer(state = {
    name: 'app root reducer'
}, action) {
    return state;
}
